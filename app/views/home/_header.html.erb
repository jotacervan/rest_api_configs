        <div class="header">
            <div class="content row">
                <div class="col-md-2">
                    <img src="/images/logo-slide.png" onclick="javascript: location.href='<%= root_path %>'" alt="Pizza Prime" title="Pizza Prime" class="logo pointer" />
                </div>
                <div class="col-md-10 text-right">
                    <ul class="menu">
                        <li <% if page == 'home' %> class="active" <% end %> ><a href="#">Home</a></li>
                        <li <% if page == 'loja' %> class="active" <% end %> ><a href="<%= loja_path %>">Unidades/Cardápios</a></li>
                        <li><a href="#">A Pizza Prime</a></li>
                        <li><a href="#">Trabalhe Conosco/Contato</a></li>
                        <li>
                            <% if session[:id].nil? %>  
                                <a href="<%= loja_path %>"><strong>Peça Agora</strong></a>
                            <% else %>
                                <a href="<%= cardapio_path(session[:state],session[:neighbor],session[:id],session[:size]) %>"><strong>Peça Agora</strong></a>
                            <% end %>
                        </li>
                    </ul>
                </div>
            </div>
        </div>