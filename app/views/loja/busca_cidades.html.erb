<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" >
        <meta name="viewport" content="width=device-width, initial-scale=1" > 
        <link href="https://fonts.googleapis.com/css?family=Titillium+Web:200,300,400,700" rel="stylesheet">
        <link rel="stylesheet" href="/css/bootstrap.css" >
        <link rel="stylesheet" href="/css/css.css" >

        <script src="https://use.fontawesome.com/7f3531401e.js"></script>

        <title>Pizza Prime</title>
    </head>
    <body>
        <%= render :partial => 'header', :locals => {:page => 'loja'} %>
        
        <div class="stores">
        	<div>
        		<div class="content center">
        			<ul class="states">
        				<% @states.each do |state| %>
        					<li <% if state == @selected %> class="active" <% end %> onclick="javascript: location.href='<%= busca_cidades_path(state) %>'">
                                <% 
                                case state 
                                when 'SP'
                                    concat 'São Paulo'
                                when 'RJ'
                                    concat 'Rio de Janeiro'
                                when 'SC'
                                    concat 'Santa Catarina'
                                when 'RS'
                                    concat 'Rio Grande do Sul'
                                when 'PR'
                                    concat 'Paraná'
                                when 'AP'
                                    concat 'Amapá'
                                else
                                    concat ''
                                end
                                
                                %>
                            </li>
        				<% end %>
        			</ul>
                    <ul class="neighbor">
                    <% if @neighborhood.length > 0 %>
                        <% @neighborhood.each do |n| %>
                            <li onclick="javascript: location.href='<%= cardapio_path(n.state,n.neighborhood,n.id,n.tamanhos.first.id) %>'">
                                <%= n.neighborhood %>
                            </li>
                        <% end %>
                    <% end %>
                    </ul>
        		</div>
        	</div>
        </div>
        <br>
        <br>
        <div class="center content">
            <h2>Selecione a Unidade:</h2>
            <br>
            <div class="row unities">
            <% if @neighborhood.length > 0 %>
                <% @neighborhood.each do |n| %>
                    <div class="col-md-3 center">
                        <div onclick="javascript: location.href='<%= cardapio_path(n.state,n.neighborhood,n.id,n.tamanhos.first.id) %>'">
                            <img src="/images/pin.png" alt="Pizza Prime" title="Pizza Prime" /><br>
                            <%= n.neighborhood %>
                        </div>
                    </div>    
                <% end %>
            <% else %>
                <h3>Não há nenhuma unidade disponível para esse estado!</h3>
            <% end %>
                
            </div>
            
        </div>
       

        <script type="text/javascript" src="/js/jquery-3.1.1.min.js"></script>
        <script type="text/javascript" src="/js/bootstrap.js"></script>
        <script type="text/javascript" src="/js/main.js"></script>
       
    </body>
</html>
